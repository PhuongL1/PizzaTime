{
  "name": "pizzatime-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --project demo-pizzatime-rules --only firestore --config ../firebase.json \"npm run test:rules\"",
    "test:rules": "node --test --test-concurrency=1 test/*.test.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.16.0"
  }
}
